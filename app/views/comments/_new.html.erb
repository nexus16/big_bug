<% if current_user %>
	<%= form_for [movie, review, @comment], remote: true do |f| %>
  <div class="form-group">
    <%= f.text_field :body, autofocus: true, class: "form-control dkm-linh-#{review.id}" %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% end %>
